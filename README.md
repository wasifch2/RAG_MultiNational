# RAG_MultiNational
This is a trained chatbot on the dataset of 3 countries which talks about divorces, inheritance, cases processed. It uses RAG to retrieve the data with Single and Multi Agent Architecture successfully and then evaluates the performance by RAGAS.
